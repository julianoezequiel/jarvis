/** @type {import('next').NextConfig} */
const nextConfig = {
  // Desativa o indicador "N" do Next.js que aparece no canto inferior da tela em dev mode
  devIndicators: false,
}

module.exports = nextConfig
